import React from "react";
import { StyleSheet, TextInput, View } from "react-native";

const Input = (props: any) => {
	return <TextInput {...props} placeholderTextColor={"gray"} />;
};

export default Input;
